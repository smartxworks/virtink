apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: kubrid-controller
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: kubrid-controller
subjects:
  - kind: ServiceAccount
    name: kubrid-controller
    namespace: kubrid-system
